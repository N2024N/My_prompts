# Notion 集成权限修复指南

## 问题诊断

根据API测试结果，你的Notion集成存在以下问题：

### ✅ 已成功完成
1. **集成已创建**：`Prompt Studio API` 集成已存在
2. **数据库已共享**：4个数据库已"连接"到集成
3. **API密钥有效**：可以成功调用Notion API

### ❌ 当前问题
1. **集成无工作区成员权限**：集成能看到数据库列表，但无法访问具体数据库
2. **数据库未正确共享**：错误信息显示"Make sure the relevant pages and databases are shared with your integration"

## 根本原因

Notion集成有两种权限级别：
1. **连接（Connection）** - 集成"知道"数据库存在（你已完成）
2. **工作区成员（Workspace Member）** - 集成有**权限访问**数据库（你缺少这个）

## 解决方案：3种修复方法

### 方法1：检查/修复集成的工作区权限（推荐）

**步骤**：
1. 打开Notion → 右上角 **Settings & Members**
2. 左侧菜单选择 **Integrations**
3. 找到 **"Prompt Studio API"** 集成
4. **重点检查**：是否能看到 **"Add to workspace"** 按钮
5. 如果有，点击并添加集成到工作区

**验证**：添加后等待1分钟，然后我可以重新测试API访问。

### 方法2：重新共享数据库给集成

如果方法1无效，尝试重新共享数据库：

**步骤**：
1. 打开 `Prompts` 数据库
2. 点击右上角 **···**（三个点）
3. 选择 **Add connections**（添加连接）
4. 在搜索框中输入 **"Prompt Studio API"**
5. 如果已存在，先**点击移除**（Remove connection）
6. 等待5秒，然后**重新添加**（Add connection）
7. 对4个数据库重复此操作：
   - `prompts` (ID: 36d3431e-c8e6-8016-9aa5-f78ff6c7e1e6)
   - `prompt_versions` (ID: 36e3431e-c8e6-8045-a413-fc20efd7a4c8)
   - `evaluations` (ID: 36e3431e-c8e6-8038-8311-c71e71cc6418)
   - `prompt_templates` (ID: 36e3431e-c8e6-8000-9298-ea5aff019272)

### 方法3：重新创建集成（最后手段）

如果以上都无效：

**步骤**：
1. 创建新集成时选择 **"Internal integration"**（而非公开集成）
2. 创建后立即添加到工作区
3. 重新共享4个数据库给新集成

## 关于你提供的页面

你提供的URL：`https://app.notion.com/p/AI-Prompt-Engineering-Studio-36d3431ec8e68022b79cc08a125b6085`

这是**AI Prompt Engineering Studio的主页面**，不是集成设置页面。

在这个页面上，你可以：
1. 查看项目概览
2. 访问链接的数据库
3. 但**无法**在这里设置集成权限

## 验证成功

修复后，我将运行测试验证。成功标志：
```
✅ Notion客户端创建成功
✅ 数据库访问成功
📝 数据库标题: [你的数据库标题]
```

## 时间线

**你的操作（2-5分钟）** + **我的测试（2分钟）** 完成后：
1. ✅ 修复Supabase客户端库问题（5分钟）
2. ✅ 进行最终集成测试（5分钟）
3. ✅ 部署到Railway（10分钟）

**总预计**：20分钟内可完成所有集成并部署

## 立即行动建议

1. **先尝试方法1**（检查集成的工作区权限）
2. **如果不行，尝试方法2**（重新共享数据库）
3. **完成后回复我**，我将立即测试

**注意**：每次更改后等待1-2分钟让Notion同步权限。